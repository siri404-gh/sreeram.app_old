/**
 *
 * @param {string} theme
 * @return {Object}
 */
export default theme => ({
  app: {
    flexGrow: 1,
    minHeight: 440,
    zIndex: 1,
    overflow: 'hidden',
    position: 'relative',
    display: 'flex',
    width: '100%',
  },
});
